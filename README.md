# Personal Website

A small static personal site for Tanishq Pauskar.

Open `index.html` directly in a browser, or serve this folder with any static file server. The site has three anchor-based tabs: About, Projects, and Math. Add new problem statements to the `problems` array in `script.js`; the archive count updates automatically.
